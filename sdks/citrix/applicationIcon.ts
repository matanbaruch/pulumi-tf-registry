// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class ApplicationIcon extends pulumi.CustomResource {
    /**
     * Get an existing ApplicationIcon resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: ApplicationIconState, opts?: pulumi.CustomResourceOptions): ApplicationIcon {
        return new ApplicationIcon(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'citrix:index/applicationIcon:ApplicationIcon';

    /**
     * Returns true if the given object is an instance of ApplicationIcon.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is ApplicationIcon {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === ApplicationIcon.__pulumiType;
    }

    /**
     * Path to the icon file. Exactly one of `raw_data` and `file_path` is required.
     */
    public readonly filePath!: pulumi.Output<string | undefined>;
    /**
     * Prepare an icon in ICO format and convert its binary raw data to base64 encoding. Use the base64 encoded string as the
     * value of this attribute. Exactly one of `raw_data` and `file_path` is required.
     */
    public readonly rawData!: pulumi.Output<string | undefined>;

    /**
     * Create a ApplicationIcon resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ApplicationIconArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: ApplicationIconArgs | ApplicationIconState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as ApplicationIconState | undefined;
            resourceInputs["filePath"] = state ? state.filePath : undefined;
            resourceInputs["rawData"] = state ? state.rawData : undefined;
        } else {
            const args = argsOrState as ApplicationIconArgs | undefined;
            resourceInputs["filePath"] = args ? args.filePath : undefined;
            resourceInputs["rawData"] = args ? args.rawData : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(ApplicationIcon.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering ApplicationIcon resources.
 */
export interface ApplicationIconState {
    /**
     * Path to the icon file. Exactly one of `raw_data` and `file_path` is required.
     */
    filePath?: pulumi.Input<string>;
    /**
     * Prepare an icon in ICO format and convert its binary raw data to base64 encoding. Use the base64 encoded string as the
     * value of this attribute. Exactly one of `raw_data` and `file_path` is required.
     */
    rawData?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a ApplicationIcon resource.
 */
export interface ApplicationIconArgs {
    /**
     * Path to the icon file. Exactly one of `raw_data` and `file_path` is required.
     */
    filePath?: pulumi.Input<string>;
    /**
     * Prepare an icon in ICO format and convert its binary raw data to base64 encoding. Use the base64 encoded string as the
     * value of this attribute. Exactly one of `raw_data` and `file_path` is required.
     */
    rawData?: pulumi.Input<string>;
}
