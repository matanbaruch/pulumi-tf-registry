// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The provider type for the splunkconfig package. By default, resources use package-wide configuration
 * settings, however an explicit `Provider` instance may be created and passed during resource
 * construction to achieve fine-grained programmatic control over provider settings. See the
 * [documentation](https://www.pulumi.com/docs/reference/programming-model/#providers) for more information.
 */
export class Provider extends pulumi.ProviderResource {
    /** @internal */
    public static readonly __pulumiType = 'splunkconfig';

    /**
     * Returns true if the given object is an instance of Provider.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Provider {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === "pulumi:providers:" + Provider.__pulumiType;
    }

    /**
     * YAML content containing the abstracted configuration. Exactly one of `configuration`, `configuration_file`, or
     * `configuration_path` must be set.
     */
    public readonly configuration!: pulumi.Output<string | undefined>;
    /**
     * Full path to YAML file containing the abstracted configuration. Exactly one of `configuration`, `configuration_file`, or
     * `configuration_path` must be set.
     */
    public readonly configurationFile!: pulumi.Output<string | undefined>;
    /**
     * Full path to directory containing one or more YAML files containing the abstracted configuration. Exactly one of
     * `configuration`, `configuration_file`, or `configuration_path` must be set.
     */
    public readonly configurationPath!: pulumi.Output<string | undefined>;

    /**
     * Create a Provider resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: ProviderArgs, opts?: pulumi.ResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        {
            resourceInputs["configuration"] = args ? args.configuration : undefined;
            resourceInputs["configurationFile"] = args ? args.configurationFile : undefined;
            resourceInputs["configurationPath"] = args ? args.configurationPath : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Provider.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * The set of arguments for constructing a Provider resource.
 */
export interface ProviderArgs {
    /**
     * YAML content containing the abstracted configuration. Exactly one of `configuration`, `configuration_file`, or
     * `configuration_path` must be set.
     */
    configuration?: pulumi.Input<string>;
    /**
     * Full path to YAML file containing the abstracted configuration. Exactly one of `configuration`, `configuration_file`, or
     * `configuration_path` must be set.
     */
    configurationFile?: pulumi.Input<string>;
    /**
     * Full path to directory containing one or more YAML files containing the abstracted configuration. Exactly one of
     * `configuration`, `configuration_file`, or `configuration_path` must be set.
     */
    configurationPath?: pulumi.Input<string>;
}
