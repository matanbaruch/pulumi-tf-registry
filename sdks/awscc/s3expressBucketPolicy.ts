// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class S3expressBucketPolicy extends pulumi.CustomResource {
    /**
     * Get an existing S3expressBucketPolicy resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: S3expressBucketPolicyState, opts?: pulumi.CustomResourceOptions): S3expressBucketPolicy {
        return new S3expressBucketPolicy(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'awscc:index/s3expressBucketPolicy:S3expressBucketPolicy';

    /**
     * Returns true if the given object is an instance of S3expressBucketPolicy.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is S3expressBucketPolicy {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === S3expressBucketPolicy.__pulumiType;
    }

    /**
     * The name of the S3 directory bucket to which the policy applies.
     */
    public readonly bucket!: pulumi.Output<string>;
    /**
     * A policy document containing permissions to add to the specified bucket. In IAM, you must provide policy documents in
     * JSON format. However, in CloudFormation you can provide the policy in JSON or YAML format because CloudFormation
     * converts YAML to JSON before submitting it to IAM.
     */
    public readonly policyDocument!: pulumi.Output<string>;

    /**
     * Create a S3expressBucketPolicy resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: S3expressBucketPolicyArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: S3expressBucketPolicyArgs | S3expressBucketPolicyState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as S3expressBucketPolicyState | undefined;
            resourceInputs["bucket"] = state ? state.bucket : undefined;
            resourceInputs["policyDocument"] = state ? state.policyDocument : undefined;
        } else {
            const args = argsOrState as S3expressBucketPolicyArgs | undefined;
            if ((!args || args.bucket === undefined) && !opts.urn) {
                throw new Error("Missing required property 'bucket'");
            }
            if ((!args || args.policyDocument === undefined) && !opts.urn) {
                throw new Error("Missing required property 'policyDocument'");
            }
            resourceInputs["bucket"] = args ? args.bucket : undefined;
            resourceInputs["policyDocument"] = args ? args.policyDocument : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(S3expressBucketPolicy.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering S3expressBucketPolicy resources.
 */
export interface S3expressBucketPolicyState {
    /**
     * The name of the S3 directory bucket to which the policy applies.
     */
    bucket?: pulumi.Input<string>;
    /**
     * A policy document containing permissions to add to the specified bucket. In IAM, you must provide policy documents in
     * JSON format. However, in CloudFormation you can provide the policy in JSON or YAML format because CloudFormation
     * converts YAML to JSON before submitting it to IAM.
     */
    policyDocument?: pulumi.Input<string>;
}

/**
 * The set of arguments for constructing a S3expressBucketPolicy resource.
 */
export interface S3expressBucketPolicyArgs {
    /**
     * The name of the S3 directory bucket to which the policy applies.
     */
    bucket: pulumi.Input<string>;
    /**
     * A policy document containing permissions to add to the specified bucket. In IAM, you must provide policy documents in
     * JSON format. However, in CloudFormation you can provide the policy in JSON or YAML format because CloudFormation
     * converts YAML to JSON before submitting it to IAM.
     */
    policyDocument: pulumi.Input<string>;
}
