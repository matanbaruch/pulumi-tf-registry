// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

export class VirtualHostNames extends pulumi.CustomResource {
    /**
     * Get an existing VirtualHostNames resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: VirtualHostNamesState, opts?: pulumi.CustomResourceOptions): VirtualHostNames {
        return new VirtualHostNames(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'pingfederate:index/virtualHostNames:VirtualHostNames';

    /**
     * Returns true if the given object is an instance of VirtualHostNames.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is VirtualHostNames {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === VirtualHostNames.__pulumiType;
    }

    /**
     * List of virtual host names.
     */
    public readonly virtualHostNames!: pulumi.Output<string[]>;

    /**
     * Create a VirtualHostNames resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: VirtualHostNamesArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: VirtualHostNamesArgs | VirtualHostNamesState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as VirtualHostNamesState | undefined;
            resourceInputs["virtualHostNames"] = state ? state.virtualHostNames : undefined;
        } else {
            const args = argsOrState as VirtualHostNamesArgs | undefined;
            resourceInputs["virtualHostNames"] = args ? args.virtualHostNames : undefined;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(VirtualHostNames.__pulumiType, name, resourceInputs, opts, false /*dependency*/, utilities.getPackage());
    }
}

/**
 * Input properties used for looking up and filtering VirtualHostNames resources.
 */
export interface VirtualHostNamesState {
    /**
     * List of virtual host names.
     */
    virtualHostNames?: pulumi.Input<pulumi.Input<string>[]>;
}

/**
 * The set of arguments for constructing a VirtualHostNames resource.
 */
export interface VirtualHostNamesArgs {
    /**
     * List of virtual host names.
     */
    virtualHostNames?: pulumi.Input<pulumi.Input<string>[]>;
}
